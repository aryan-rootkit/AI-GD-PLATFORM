# Backend origin only — no trailing slash, no /api (unless your server really mounts under /api).
NEXT_PUBLIC_API_URL=https://ai-gd-platform-42n4.onrender.com
NEXT_PUBLIC_SOCKET_URL=https://ai-gd-platform-42n4.onrender.com

# Set to 1 to log every request URL and error payloads (dev also logs by default).
# NEXT_PUBLIC_DEBUG_API=1

# On the Node backend (Render), set CLIENT_ORIGIN to this Vercel URL (comma-separated for previews).
